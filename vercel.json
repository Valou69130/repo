{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["cdg1"],
  "installCommand": "npm install && npm install --prefix collateral-app && npm install --prefix collateral-api && cd collateral-api && npm rebuild better-sqlite3",
  "buildCommand": "cd collateral-app && npm run build",
  "outputDirectory": "collateral-app/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...route]" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
